<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.koreait.dooboo.member.dao.MemberDAO">
	<insert id="join" >
		INSERT INTO MEMBER 
		VALUES
		(Member_SEQ.NEXTVAL,
		#{name},
		#{phone},
		#{memberId},
		#{password},
		#{email},
		#{nickname},
		#{gender}, 
		SYSDATE, 
		null , 
		0 , 
		#{city}, 
		#{region}, 
		#{location},
		#{birthday},
		null) <!-- status 0 = 정상   , 1= 삭제 -->
	</insert>
	
	<select id="login" resultType="com.koreait.dooboo.member.dto.MemberDTO">
		SELECT * FROM MEMBER WHERE MEMBER_ID = #{memberId} AND PASSWORD= #{password} AND STATUS = 0
	</select>
	<update id="updateInfo">
		UPDATE MEMBER SET NAME = #{name} , NICKNAME = #{nickname} WHERE MEMBERNO = #{memberNo}
	</update>
	
</mapper>