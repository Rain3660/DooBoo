<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.koreait.dooboo.product.dao.ProductDAO">

<sql id="startPaging">
  	 	SELECT
	  	 	PAGE_B.*
  	 	FROM(
	  	 	SELECT
		  	 	ROWNUM AS RNUM
		  	 	,PAGE_A.*
	  	 	FROM(  	 	
</sql>
<sql id="endPaging">
			) PAGE_A
			WHERE ROWNUM <![CDATA[<=]]> #{endPage}
		) PAGE_B
		WHERE RNUM <![CDATA[>=]]> #{startPage}
</sql>
<sql id="productColumns">
		  PRODUCT_NO
		, TITLE
		, PRICE
		, HIT
		, CONTENT
		, STATUS
		, ADDRESS
		, REG_NO
		, MOD_NO
		, REG_DATE
		, MOD_DATE
	</sql>
	
	<resultMap id="boardResult" type="com.koreait.dooboo.communityboard.dto.CommunityBoardDTO">
	    <result property="rnum" column="RNUM"/>
	    <result property="boardNo" column="BOARD_NO"/>
	    <result property="title" column="TITLE"/>
	    <result property="content" column="CONTENT"/>
	    <result property="hit" column="HIT"/>
	    <result property="regNo" column="REG_NO"/>
	    <result property="modNo" column="MOD_NO"/>
	    <result property="regDate" column="REG_DATE"/>
	    <result property="modDate" column="MOD_DATE"/>
	    <result property="nickname" column="NICKNAME"/>
	    <collection property="productImageList" column="BOARD_NO" javaType="java.util.ArrayList" ofType="com.koreait.dooboo.product.dto.ProductImageDTO" select="selectBoardFileList"/>
	</resultMap>
	 

	<select id="selectBoardFileList" resultType="com.koreait.dooboo.product.dto.ProductImageDTO">
		SELECT * FROM PRODUCT_IMAGE WHERE PRODUCT_NO = #{productNo}
	</select>

	<select id="selectProductList" parameterType="com.koreait.dooboo.product.dto.ProductDTO" resultType="com.koreait.dooboo.product.dto.ProductDTO">
  	 	<include refid="startPaging"/>
	  		SELECT
	  			  ROWNUM
				, P.PRODUCT_NO
				, P.TITLE
				, P.PRICE
				, P.HIT
				, P.CONTENT
				, P.STATUS
				, P.ADDRESS
				, P.REG_NO
				, P.MOD_NO
				, P.REG_DATE
				, P.MOD_DATE
			FROM
				PRODUCT P
			LEFT OUTER JOIN MEMBER M
            ON REG_NO = M.MEMBER_NO
			WHERE DEL_YN = 'N'
			ORDER BY
				P.PRODUCT_NO DESC,
				P.REG_DATE DESC 
		<include refid="endPaging"/>				
	</select>
	
	<update id="updateHit" parameterType="long">
		UPDATE
			PRODUCT 
		SET 
			HIT = HIT + 1
		WHERE
			PRODUCT_NO = #{productNo}
	</update>
	
	<select id="selectProductTotalCount" parameterType="com.koreait.dooboo.product.dto.ProductDTO" resultType="int">
		SELECT
			COUNT(*)
		FROM
			PRODUCT
		WHERE
			del_yn = 'N'
	</select>

	<!-- 판매상품을 등록한다 -->
	<insert id="insertProduct">
		INSERT INTO PRODUCT VALUES
		(PRODUCT_SEQ.NEXTVAL , #{title} , #{price} , 0 , #{content} , 1
		<!-- 추후에 #{address} 로 변경 예정 -->
		  , '경기도 고양시'
		  , #{regNo} , null , SYSDATE , null)
	</insert>
	
	<select id="selectProductDetail" parameterType="long" resultType="com.koreait.dooboo.product.dto.ProductDTO">
		SELECT
			ROWNUM ,
			COM.BOARD_NO ,
			p.TITLE ,
			p.CONTENT ,
			p.HIT ,
			p.REG_NO,
			p.MOD_NO,
			p.REG_DATE,
			p.MOD_DATE,
			M.NICKNAME
		FROM
			PRODUCT P 
			LEFT OUTER JOIN MEMBER M
            ON REG_NO = M.MEMBER_NO
		WHERE
			PRODUCT_NO = #{productNo}
	</select>

	<update id="updateProduct" parameterType="com.koreait.dooboo.product.dto.ProductDTO">
		UPDATE PRODUCT
		SET
			  MOD_DATE = SYSDATE
			, TITLE = #{title}
			, CONTENT = #{content}
		WHERE
			PRODUCT_NO = #{productNo}
	</update>
	
	<!-- 업로드한 이미지 파일을 관리한다 -->
	<insert id="insertProductImage" parameterType="com.koreait.dooboo.product.dto.ProductImageDTO">
		INSERT INTO PRODUCT_IMAGE
		(	
			 PRODUCT_NO
			,FILE_NO
			,FILE_PATH
			,FILE_NAME
			,REG_NO
			,REG_DATE
		)VALUES(
			  #{productNo}
			, PRODUCTIMAGE_SEQ.NEXTVAL 
			, #{filePath} 
			, #{fileName} 
			, #{regNo} 
			, SYSDATE
		)
	</insert>
	<!-- 판매등록한 상품을 삭제한다. -->
	<delete id="deleteProduct">
		DELETE FROM PRODUCT WHERE PRODUCT_NO = #{param1}
	</delete>
	<!-- <select id="selectProductImageByProductNo" resultType="com.koreait.dooboo.product.dto.ProductImageDTO">
		SELECT * FROM PRODUCT_IMAGE WHERE PRODUCT_NO = #{param1}
	</select>
	판매등록한 상품을 삭제할 때 , 첨부한 이미지가 있다면 삭제한다.
	<delete id="deleteProductImage">
		DELETE FROM PRODUCT_IMAGE WHERE PRODUCT_NO = #{param1}
	</delete>
 -->
</mapper>